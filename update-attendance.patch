<<<<<<< SEARCH
    const update = {
      $setOnInsert: {
        tenantId: ctx.school_id,
        liveClassId: new mongoose.Types.ObjectId(liveClassId),
        studentId: new mongoose.Types.ObjectId(studentId),
        joinTime: new Date(),
        status: "PRESENT",
      }
    };
=======
    const update = {
      $setOnInsert: {
        tenantId: ctx.school_id,
        liveClassId: new mongoose.Types.ObjectId(liveClassId),
        studentId: new mongoose.Types.ObjectId(studentId),
        status: "PRESENT",
      },
      $set: {
        joinTime: new Date() // Always update join time to handle reconnections
      }
    };
>>>>>>> REPLACE
