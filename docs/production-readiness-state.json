{
  "mission": "production-readiness remediation loop",
  "audit_iteration": 5,
  "last_updated": "2026-09-04T22:15:00Z",
  "environment": {
    "backend_running": true,
    "backend_mode": "postgres-backed dual pool + Redis distributed limiter/cache",
    "postgres_running": true,
    "redis_running": true,
    "docker_available": true,
    "note": "PostgreSQL 16 + Redis 7 running locally in Docker; all 37 migrations applied clean from an empty database"
  },
  "findings": {
    "F-01_signup_privileged_role": {
      "status": "CLOSED",
      "evidence": "role allowlist + superadmin-gated SkipOTP; skip_otp_test.go; live battery: admin/super_admin signup 400 no token, anonymous owner forced through OTP, full OTP signup completes"
    },
    "F-02_rls_inert": {
      "status": "CLOSED",
      "fix": "000035 FORCE RLS + unified fail-closed policies with documented __global__/empty escape for the trusted sync layer; 000036 least-privilege school_runtime role (bootstrap superuser is undemotable in PG16); persistence dual pool — RuntimePool() SET ROLE school_runtime on every connection, request handlers moved off the owner pool; 000037 extended FORCE coverage to identity/roster/billing tables (51 tables total)",
      "evidence": "scripts/rls_isolation_battery.sh 24/24 PASS on real PG16 (read/update/delete/insert/join/aggregate isolation incl users/parents/subjects); app boots against PG with all module routes 200 through the runtime pool; integration suite green vs real PG"
    },
    "F-03_parent_directory_leak": {
      "status": "CLOSED",
      "fix": "tenant-wide student-directory fallback removed; explicit links only"
    },
    "F-04_websocket_hijacker": {
      "status": "CLOSED",
      "fix": "logging.go + metrics.go preserve Hijacker/Flusher/Pusher/ReaderFrom; hub double-close panic + multi-tab channel bug fixed; websocket_realstack_test.go + hub shutdown race test pass under -race"
    },
    "F-05_plaintext_passwords": {
      "status": "CLOSED",
      "evidence": "PasswordHash-only plumbing (bcrypt); no password field in owner/superadmin/session/login JSON (live-verified)"
    },
    "F-06_session_security": {
      "status": "CLOSED",
      "evidence": "8h/30d TTLs, cookie flags, no-store session, scoped ws-ticket exchange, full JWT in ws query rejected by default (live-verified)"
    },
    "F-07_dev_jwt_fallback": {
      "status": "CLOSED",
      "evidence": "random per-boot dev secret; production validation requires JWT_SECRET/DATABASE_URL/ALLOWED_ORIGINS/BREVO/COOKIE_SECURE/METRICS_TOKEN; rejects ALLOW_WS_TOKEN_QUERY and insecure origins"
    },
    "F-09_metrics_auth": {
      "status": "CLOSED",
      "evidence": "constant-time Protected middleware; live: 401 without token, 200 with Bearer or ?token="
    },
    "F-11_class_fees_metadata_leak": {
      "status": "CLOSED",
      "fix": "ListClassFees tenant-scopes class lookup"
    },
    "F-16_source_maps": {
      "status": "CLOSED",
      "evidence": "sourcemap opt-in via env (school app, default off); super-admin + landing verified; all three production builds emit 0 .map files"
    },
    "NEW_pg_bootstrap_subscription_lockout": {
      "status": "CLOSED",
      "fix": "EnsureBootstrapUsers seeds an active trial subscription for the bootstrap school so SubscriptionGate does not 403-lock every module on fresh PG deployments"
    },
    "NEW_fee_worker_on_conflict": {
      "status": "CLOSED",
      "fix": "GenerateFees ON CONFLICT target now matches the partial unique index (school_id, student_id, academic_year_id, month, year) WHERE month <> '' (SQLSTATE 42P10 on real PG)"
    },
    "NEW_pg_integration_fixtures": {
      "status": "CLOSED",
      "fix": "helpers seed FK actor rows (users, fee_types), dashboard test refreshes matviews + purges stale cache; full integration suite passes against real PG+Redis"
    },
    "rate_limiting_distributed": {
      "status": "CLOSED",
      "fix": "RedisAuthLimiter (go-redis v9, fixed window, SET NX EX atomic key creation so keys always carry TTL; in-memory fallback when Redis is down), wired for auth endpoints when REDIS_URL is configured; 5 unit tests + live: 10 logins allowed then 429 with rl:auth:60:127.0.0.1 key + TTL in Redis"
    },
    "rls_coverage_expansion": {
      "status": "CLOSED",
      "fix": "000037 extends FORCE RLS to users, parents, student_parents, subjects, academic_years, sections, campuses, owner_schools, schools, teacher_attendance, school_settings, dummy_data_batches, pending_signups, subscriptions (14 more tables, 51 total); battery extended to users/parents/subjects probes — 24/24 PASS"
    }
  },
  "tests": {
    "backend_full_suite": "PASS, 0 failures, go vet clean",
    "integration_pg_redis": "PASS vs real PostgreSQL 16 + Redis 7",
    "integration_race": "PASS (-race)",
    "rls_isolation_battery": "24/24 PASS",
    "rate_limiter_unit": "PASS (5 tests incl Redis-down fallback)",
    "live_http_battery": "PASS (signup policy matrix, OTP loop, session hardening, ws-ticket, metrics auth, distributed 429, no plaintext)",
    "school_frontend": "76 passed, build PASS (0 source maps)",
    "super_admin_build": "PASS, 0 source maps",
    "landing_build": "PASS, 0 source maps",
    "migrations": "37/37 applied clean from an empty database"
  },
  "remaining_risks_non_blocking": [
    "RLS empty/__global__ context escape is deliberate for the trusted MemStore->PG sync layer; request paths that adopt SET LOCAL are DB-enforced. Junction tables without a direct school_id column (class_teachers, student_subjects, ...) remain app-layer scoped.",
    "Deployment secrets (JWT_SECRET, BREVO_API_KEY) live in repo .env files; production compose must supply real env values.",
    "Backend currently runs on :8080 in PG-backed + Redis mode against the local Docker PostgreSQL/Redis; containers left running for further verification."
  ],
  "next_actions": [
    "Optional: commit review of this remediation set (all changes currently uncommitted)"
  ]
}
